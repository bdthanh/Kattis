#include <iostream>

using namespace std; 

int main() {
    long tc; cin >> tc;
    for (long i = 0; i < tc; i++) {
        long n, m; cin >> n >> m;
        cout << n-1 << '\n';
        if (i!=tc-1) {
            for (long j = 0; j < m; j++) {
                long a,b; cin >> a >> b; 
            }
        }
    
    }
    
    return 0;
}